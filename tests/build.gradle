apply plugin: 'java-library'
apply plugin: 'eclipse-wtp'


test {
    includes = [ "**/*Test.class", "**/*Suite.class" ]
    excludes = [ "**/*\$*.class" ]
    scanForTestClasses = false
}

dependencies {
    api project(':msl-core')
    api 'junit:junit:latest.release'
}